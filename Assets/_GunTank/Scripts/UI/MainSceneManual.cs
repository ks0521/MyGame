using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MainSceneManual : MonoBehaviour
{
    // Start is called before the first frame update
    public void OnClicked()
    {
        gameObject.SetActive(false);
    }
}
